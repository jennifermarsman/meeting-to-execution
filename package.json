{
  "name": "the-next-step",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A meeting-derived, local-first weight and habit journey prototype.",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node scripts/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test",
    "status": "node scripts/generate-status.mjs",
    "issues:publish": "node scripts/publish-issues.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
